{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "org",
  "title": "Org",
  "description": "An organisation record stored in the 'orgs' collection, keyed by orgId.",
  "type": "object",
  "required": ["orgId", "name", "createdBy", "createdAt"],
  "properties": {
    "orgId": {
      "type": "string",
      "description": "UUID identifying the organisation"
    },
    "name": {
      "type": "string",
      "description": "Human-readable organisation name (unique across the instance)"
    },
    "createdBy": {
      "type": "string",
      "description": "userId of the user who created the org"
    },
    "createdAt": {
      "type": "string",
      "description": "ISO 8601 creation timestamp"
    }
  },
  "additionalProperties": false
}
